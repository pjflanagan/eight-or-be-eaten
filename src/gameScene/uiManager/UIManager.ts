import { GameScene } from "../gameScene/scene";

export class UIManager {
  scene: GameScene;

  // health bar

  constructor(scene: GameScene) {
    this.scene = scene;
  }

  initializeUI() {

  }
}